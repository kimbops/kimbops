배울 내용

* 입력값의 흐름

[사용자]
아이디/비밀번호 입력
      ↓
[클라이언트(브라우저)]
입력값을 HTTP 요청(Request)에 담음
      ↓
[인터넷]
요청 전송
      ↓
[서버]
req 객체로 입력값 받음
예: req.body.id
      ↓
[서버 처리]
- 로그인 확인
- DB 조회 (데이터 베이스 조회)
- 조건 검사
      ↓
[서버]
결과를 HTTP 응답(Response)에 담음
예: res.send("로그인 성공")
      ↓
[인터넷]
응답 전송
      ↓
[클라이언트(브라우저)]
응답 데이터 받음
      ↓
[사용자]
결과 화면 확인

(개인적인 궁금증)데이터 베이스 조회란?

- 데량의 데이터 속에서 필요한 순간에, 필요한 조건으로, 원하는 형태의 정보만 빠르고 효율적으로 찾아내기 위함

* 클라이언트 입력과 서버 처리
-클라이언트 입력 과정
① 사용자가 입력창에 값 입력
↓
② 브라우저가 입력값 저장
↓
③ 로그인 버튼 클릭
↓
④ 브라우저가 HTTP 요청(Request) 생성
↓
⑤ 입력값을 요청 안에 담음
↓
⑥ 서버 주소로 전송

ex) http
POST /login HTTP/1.1

id=admin&pw=1234

여기서 POST는 데이터를 서버로 보냄
/login -> 로그인 기능 주소
id=admin&pw=1234는 사용자가 입력한 데이터 

-서버 입력 과정

[사용자]
값 입력
↓
[브라우저]
HTTP 요청 생성
↓
입력값 저장
↓
[서버]
req로 데이터 받기
↓
입력값 분석
↓
DB/조건 검사
↓
결과 생성
↓
res로 응답 전송
↓
[브라우저]
응답 출력
↓
[사용자]
결과 확인


* XSS 기초


1. attacker가 게시판에 악성코드가 담긴 게시물을 올림
2. 사용자가 게시물을 들어가면
3. 해커에게 쿠키와 세션이 들어감



XSS 종류 3개가 있음
1. Reflected XSS 공격(반사형  XSS) - URL을 통해 즉시 실행

ex) URL 파라미터, 검색어, 에러 메시지 등이 포함
방법 : 공격자는 악성 스크립트가 삽입된 URL을  이메일이나 메신저로 전달하여 클릭 유도 -> 해당 URL 접속하는 순간, 세션 쿠키 탈취나 악성 사이트로 이동

Stored XSS ( 저장형 XSS) - 서버(DB)에 저장되어 다른 사용자에게 전달되면서 반복 실행

ex) 게시판 댓글, 사용자 프로필, 공지사항 입력란 등에서 주로 발생

특징 : 한번의 공격으로 다수 사용자에게 영향을 미침, 삭제되지 않는 한 계속 실행 됨 그래서 위험도가 제일 높음

DOM 기반 XSS – 클라이언트 측 스크립트에서 발생

즉 XSS는 서버 응답이 아니라, 클라이언트 한마디로 자바스크립트가 DOM을 처리하는 과정에서 발생하는 취약점

특징 : 브라우저에서 location, document.URL 등의 값을 통해 직접 참조된 후, innerHTML, document.write, eval 같은 DOM API를 통해 화면에 반영되면서 악성 스크립트가 실행


**Reflected 와 Stored는 서버가 사용자 입력값을 검증없이 응답 페이지에 포함시키는 과정에서 발생, 서버가 생성한 HTML에 악성 스크립트가 포함되어 브라우저로 전달되는 것이 특징, 

반면 DOM은 서버 응답에는 문제가 없음 하지만 클라이언트가 사용자 입력값을 DOM에 반영하는 과정에서 발생


dreamhack에 1,2 두 개가 있는데 무슨 종류가 사용됬는지


* SQL Injection 기초

공격 과정
1. 표적 식별 : 공격자는 먼저 사용자 입력값을 서버로 보내는 웹사이트 기능을 찾음
ex) 로그인 창, 검색 창, 문의 양식 등이 대상이됨
특히 입력값 검사가 제대로 되지 않는 곳을 노림

2. 악성 코드 주입 : 취약점이 확인되면 공격자는 입력 필드에 악성 SQL코드를 주입.

ex) OR 1=1 이는 항상 참으로 만듬

3. 악성 코드 실행 : 애플리케이션이 사용자 입력을 정제하거나 검증하지 않는 경우, 공격자의 SQL 코드가 유효한 쿼리와 함께 데이터베이스에 의해 실행됩니다

-검증하지 않은게 무슨말이냐 
-> 검문소를 두지 않아 그냥 들어오는거 받음
ex) 아이디 창에 대놓고 공격 코드를 적어보냈는데, 서버가 걸리지 않은 상황

-데이터베이스에 의해 실행은 무엇인가
-해커가 보낸 나쁜 명령어가 원래 짜둔 명령어와 합쳐져 진짜 명령어인줄 알고 그대로 실행을 함

4. 데이터 활용 : 데이터 베이스의 권한을 얻어 활용 가능
ex) 데이터베이스 조작, 권한 상승

* 필터링과 검증의 차이

웹 필터링이란? 
-> 사용자가 인터넷에서 탐색하는 위치를 모니터링하고 관리하여 조직에서 잠재적인 위협으로부터 보호, 회사 정책을 시행하기 위해 웹 트래픽을 허용 또는 차단

그러면 중요한 이유가 뭐냐

1. 멀웨어 보호 : 피싱 및 기타 악성 사이트를 사용하여
-피싱? : 악의적인 행위자가 신뢰할 수 있는 사람이나 단체로 가장하여 메시지를 보내는 사이버 보안 공격

2. 데이터 보안 : 피싱 사이트는 일반적으로 사용자 자격 증명 및 기타 중요한 데이터를 훔치기 위해 존재 이러한 페이지에 대하여 액세스를 차단하여 데이터가 유출되거나 침해될 위험을 제한

3.규제 컴플라이언스 : 기업은 특정 유형의 데이터를 무단 액세스로부터 보호하도록 의무화함
이를 웹 필터링을 통해 조직은 보호된 데이터를 훔치려고 할 가능성이 있는 사이트나 의도하지는 않더라도 데이터가 유출 가능한 사이트에 대해 액세스를 관리 함

4. 정책 시행 : 웹 사용에 대한 회사 정책에 사용



그럼 웹 필터 유형에 뭐가 있는지

-목록 허용
모든 웹 트래픽이 목록과 비교되고 목록에 포함되지 않은 대상이 있는 모든 요청은 삭제가 됨
-차단 목록
사용자가 방문하지 말아야 할 사이트를 나열, 차단 목록을 사용하면 모든 트래픽이 검사되고 목록의 대상에 대한 모든 트래픽이 삭제됨
-콘텐츠 필터링
웹 페이지의 콘텐츠를 기반하여 트래픽을 허용할지 또는 차단할지를 결정하는 것
-DNS 필터링
DNS 조회 요청을 모니터링하고 정책에 따라 트래픽을 허용하거나 차단
-URL 필터링
웹 요청에 포함된 URL을 검사
-콘텐츠 필터링
웹 페이지의 콘텐츠를 확인
-클라이언트 쪽 필터링
사용자 컴퓨터에 설치된 소프트웨어에 의해서 수행됨. 
-서버쪽 필터링




* 안전한 처리 방식의 기본 개념

정보보안의 3요소를 보장하고, 접근 통제 및 암호화를 통해 데이터를 안전하게 관리

3요소
기밀성 – 인가된 사람만 정보에 접근할 수 있도록 보호
무결성 – 인가되지 않은 변경이나 훼손 없이 데이터의 정확성과 일관성 유지
가용성 – 인가된 사용자가 필요할 때 언제든 정보와 자원에 접근할 수 있도록 보장

기본 수칙
최소 권한 부여 : 업무상 반드시 필요한 데이터만 수집 그리고 접근 권한은 필요한 만큼만 부여

데이터 암호화 : 민감한 정보를 저장 할때는 암호화 해야하고 통신 구간에는 안전한 프로토콜 사용

접근 통제 및 인증 : 강력한 비밀번호, 다중 인증

주기적 감사 및 백업 : 접속 기록을 정기적으로 점검, 데이터 손실을 대비하여 안전하곳에 정기적으로 백업

안전한 파기 : 목적이 달성되거나 보유 기간이 자날 경우 복구 할수 없도록 안전하게 파기



핵심 질문

입력값은 어디에서 들어오고 어디로 이동하는가?

브라우저에서 들어오고 서버로 이동을 한다

* HTML에 입력값이 그대로 출력되면 어떤 문제가 생기는가?

XSS 공격이 발생할 수 있다
왜냐
-> 브라우저가 코드로 해석할수 있기 때문에 위험함

* SQL 쿼리에 입력값이 그대로 들어가면 어떤 문제가 생기는가?

SQL Injection 공격이 발생 할수 있음
왜냐
-> 입력값 그대로 SQL을 넣으면 사용자의 입력이 데이터가 아니라 명령어처럼 동작 할수 있기 때문에

* 필터링만으로 안전하다고 말할 수 있는가?

아니다
왜냐
->이것만으로 하면 완전히 안전하다고 볼수 없음
우회기법이 많음
->공백 문자 우회
->논리 연산자, 비교 연산자 우회
->함수 우회
->주석처리
->싱글 쿼터(‘) 우회


일부 문자만 막아서는 차단이 불가

 Week 2 - 입력값 기반 취약점

