XSS
### 1. XSS란 무엇인가?

**개념**
웹 애플리케이션의 취약점을 이용해, 공격자가 **웹사이트에 악성 스크립트를 심어두고 그 페이지를 보는 다른 사용자의 브라우저에서 실행되게** 만드는 공격. 게시판·웹메일 등에 자바스크립트 같은 코드를 삽입해 개발자가 의도하지 않은 기능이 작동하게 한다.

**핵심 원리**

- 공격자가 피해자를 직접 공격하는 게 아니라 **웹사이트를 매개**로 한다.
- 대부분의 웹 해킹이 서버를 노리는 것과 달리, XSS는 **클라이언트(사용자)를 대상으로** 한 공격이다.

### 2. 공격 동작 흐름

1. 공격자가 게시판 등에 악성 스크립트가 담긴 게시물(또는 악성 URL)을 올린다.
2. 사용자가 그 페이지에 접속하거나 링크를 클릭한다.
3. 사용자의 브라우저에서 스크립트가 실행되어 쿠키·세션이 공격자에게 넘어간다.

### 3. XSS의 종류

**Stored XSS (저장형)**

- 악성 스크립트가 **서버 DB·파일 등에 저장**됨
- 게시판 댓글, 사용자 프로필, 후기, 공지사항 입력란 등이 표적
- 피해자가 그 페이지를 **열기만 해도** 당함 (별도 유도 불필요)
- 한 번 심으면 모든 방문자가 피해자가 되고, 삭제 전까지 계속 실행되므로 **위험도가 가장 높음**

**Reflected XSS (반사형)**

- 악성 스크립트가 **URL 파라미터·검색어·에러 메시지** 등에 들어있음
- 서버가 그 입력을 응답 페이지에 그대로 "반사"해서 돌려줌 (서버에 저장 X)
- 피해자가 그 **링크를 클릭해야** 당함 → 피싱 메일·메신저·SNS 등으로 유도
- 단독으로는 눈치채기 쉬워 Click Jacking, Open Redirect 같은 다른 취약점과 연계되기도 함

**DOM-based XSS**

- 서버 응답에는 문제가 없음. **브라우저(JavaScript)가 DOM을 처리하는 과정**에서 발생
- `location`, `document.URL` 등의 값을 `innerHTML`, `document.write`, `eval` 같은 DOM API로 화면에 직접 반영할 때 터짐
- 서버 응답에는 페이로드가 안 보일 수도 있음

> **서버측 vs 클라이언트측 구분**
> 
> - **Stored / Reflected**: 서버가 사용자 입력을 검증 없이 응답 HTML에 포함시키며 발생 → 악성 스크립트가 **서버가 만든 HTML**에 담겨 브라우저로 전달됨
> - **DOM-based**: 서버 응답은 멀쩡하고, **클라이언트가 입력을 DOM에 반영**하는 과정에서 발생

### 4. 공격 성공 시 가능한 일 (위험성)

- **쿠키/세션 ID 탈취** → 정상 사용자인 척 로그인 가로채기 (세션 ID가 쿠키에 있으면 특히 위험)
- **피해자 명의로** 글 작성·수정·삭제 등 임의 기능 수행
- **가짜 로그인 창**을 띄워 비밀번호 빼내기 / 거짓 페이지 노출(`<img>` 등으로 페이지 변조) → 개인정보 유출
- **악성 사이트로 리다이렉트** → 악성코드(트로이 목마 등) 다운로드 유도
- **시스템 통제권 획득**: 미패치 취약점 공격 코드와 결합 시 사용자 PC 장악 → 조직 내부망으로 악성코드 확산, 주요 정보 탈취 가능

### 5. 방어법

| 단계 | 방법 | 설명 |
| --- | --- | --- |
| 입력 | 필터링 | `<`, `>`, `"`, `'` 같은 특수문자 차단 (보조 수단) |
| **출력** | **이스케이프 처리** | **화면 표시 시 `<` → `&lt;`, `>` → `&gt;` 등으로 변환** |
| 추가 | CSP 설정 | 신뢰하는 도메인의 스크립트만 실행 허용 |
| 추가 | HttpOnly 쿠키 | JavaScript로 쿠키 못 읽게 막아 탈취 방지 |
| 추가 | `innerHTML` 대신 `textContent` | 입력값을 코드가 아닌 텍스트로만 취급 |

> ⚠️ XSS는 **IPS·IDS·방화벽으로는 막기 어렵다.** 입력/출력 단계의 처리가 핵심이다.
💡 **가장 중요한 포인트**: 입력 검증만으론 부족하고, **출력할 때 이스케이프**가 제일 중요하다.
>

SQL Injection
1. SQL Injection이란?
개념
코드 인젝션의 한 기법으로, 공격자가 입력값에 SQL 문법을 끼워넣어 웹사이트가 데이터베이스에 보내는 쿼리를 조작하는 공격. 기존 쿼리문을 변조해 인가되지 않은 데이터 열람·조작 등을 수행할 수 있다. (SQL 삽입, SQL 주입으로도 불림)
2. 발동 원리

서버(개발자 코드)에는 SQL 쿼리 "틀" 이 있다.
사용자가 입력하면 서버가 그 틀에 입력값을 끼워 넣어 완성된 쿼리를 만들고 DB에서 실행한다.
공격자가 입력값에 SQL 문법을 섞으면 → 쿼리의 구조 자체가 바뀌어 의도하지 않은 명령이 실행된다.


쉽게 말하면: 해커가 보낸 나쁜 명령어가 원래 짜둔 명령어와 합쳐지고, DB는 그걸 진짜 명령어인 줄 알고 그대로 실행한다.

3. 공격 과정 (단계별)

표적 식별 — 사용자 입력값을 서버로 보내는 기능을 찾는다. (로그인 창, 검색 창, 문의 양식 등) 특히 입력값 검사가 제대로 안 되는 곳을 노린다.
악성 코드 주입 — 취약점이 확인되면 입력 필드에 악성 SQL을 넣는다. (예: OR 1=1 → 조건을 항상 참으로 만듦)
악성 코드 실행 — 애플리케이션이 입력을 정제·검증하지 않으면, 공격자의 SQL이 정상 쿼리와 합쳐져 DB에서 실행된다.

"검증하지 않는다" = 검문소를 안 둬서 들어오는 걸 그냥 받음 → 아이디 창에 대놓고 공격 코드를 적어 보냈는데 서버가 안 걸러낸 상황


데이터 활용 — 획득한 DB 권한으로 데이터 조작, 권한 상승 등을 수행한다.

4. 정상 동작 vs 공격 예시
정상
sqlSELECT * FROM users WHERE username = 'kim' AND password = '1234'
공격 (admin 로그인 우회)
sqlSELECT * FROM users WHERE username = 'admin'--' AND password = '1234'
→ -- 뒤는 SQL 주석이라 무시됨 → 비밀번호 검증이 통째로 사라짐 → admin 계정 로그인 성공
5. 자주 사용되는 SQL 문자열
문자의미'문자 데이터 구분 기호;쿼리 구분 기호--, #한 줄 주석 (--: Oracle·MSSQL / #: MySQL)/* */구간 주석||문자 연결 (Oracle 전용)
6. SQL Injection의 종류

In-band SQLi — 공격 결과가 화면에 바로 보임 (가장 기본)

Error-based: DB 오류 메시지에서 정보를 빼냄 → 테이블명·필드명·레코드 값 등 DB 구조 파악에 활용
Union-based: UNION 연산자로 다른 테이블 데이터를 합쳐서 가져옴 (단, 컬럼 개수와 데이터 형식이 같아야 함)


Blind SQLi — DB 오류 메시지를 출력하지 않도록 막아둔 경우, 결과를 직접 못 보고 참/거짓 응답이나 응답 시간 차이로 데이터를 추측
Out-of-band SQLi — DNS 같은 외부 채널로 데이터를 빼냄
Stored Procedure SQLi — 보안상 취약한 프로시저를 이용해 셸 실행, 레지스트리 조작, 서비스 시작/중지, 시스템 정보 획득 등 시스템 레벨 공격
NoSQL Injection — MongoDB 같은 NoSQL 데이터베이스를 대상으로 한 공격

7. 공격 성공 시 영향

비밀번호, 신용카드, 개인정보 등 민감 데이터 유출
데이터 변조·삭제로 인한 서비스 무결성 훼손
백도어 설치로 장기간 침해가 발각되지 않을 위험
권한 상승 / DB 조작
유출 사고로 인한 평판 손상과 규제 벌금

8. 방어법
1. Prepared Statement (매개변수화 쿼리) — 가장 중요
쿼리 틀과 입력값을 분리해서 DB로 보내, 입력값이 아무리 이상해도 명령이 아닌 데이터로만 취급되게 한다.
python# 위험: 문자열 연결
query = "SELECT * FROM users WHERE id = '" + user_input + "'"

# 안전: 매개변수화 쿼리
cursor.execute("SELECT * FROM users WHERE id = ?", (user_input,))
2. ORM 사용 — 자체적으로 안전한 쿼리를 생성 (Django ORM, SQLAlchemy 등)
3. 입력 검증 — 정규 표현식 등으로 화이트리스트 기반 필터링 (허용된 값만 통과). 단, 입력값을 직접 SQL로 넘기지 않는 것이 전제.
4. 이중 검증 — 클라이언트(JS)에서 한 번 거르되, 서버 측은 클라이언트 필터가 없다고 가정하고 한 번 더 필터링한다.
5. 최소 권한 원칙 — DB 계정에 꼭 필요한 권한만 부여 (전체 삭제 권한 X). 유저별 접근 권한·사용 가능 명령어를 제한하면 피해를 최소화할 수 있다.
6. 에러 메시지 숨기기 — 공격자에게 DB 구조·오류 정보를 노출하지 않는다.
7. 연결 정보 보호 — DB 연결 문자열은 가급적 config 파일에 평문 저장하지 않고 암호화한다.

💡 핵심: 입력 필터링만으로는 부족하다. 쿼리 틀과 입력값을 분리하는 Prepared Statement가 가장 확실한 방어다.

- JSON 타입이 무엇인가?
    
    ### 개념
    
    데이터를 **텍스트 형태로 주고받기 위한 표준 포맷**. 거의 모든 웹 API에서 사용한다.
    
    JavaScript Object Notation의 약자로 자바 스크립트 객체 표기법이다.
    (데이터를 쉽게 교환하고 저장하기 위한 텍스트 기반의 데이터 교환 표준)
    
    ### 왜 쓰는가?
    
    - 사람이 **읽고 쓰기 쉬움**
    - 컴퓨터가 **파싱(해석)하기 쉬움**
    - XML보다 **간결하고 가벼움**
    - **언어 독립적** — Python, Java, JavaScript 등 어디서든 사용 가능
    
    ### 기본 구조
    
    - `key: value` 쌍으로 구성
    - 데이터는 **쉼표(,)** 로 구분
    - 객체는 **중괄호 `{}`**, 배열은 **대괄호 `[]`** 로 감쌈
    - key는 **반드시 큰따옴표(`"`)** 로 감싸야 함
    
    ### 데이터 타입 (6가지)

| 타입 | 예시 | 설명 |
| --- | --- | --- |
| 문자열 (String) | `"hello"` | 반드시 큰따옴표 |
| 숫자 (Number) | `123`, `3.14` | 정수, 소수 모두 가능 |
| 불리언 (Boolean) | `true`, `false` | 참/거짓 |
| 객체 (Object) | `{ "key": "value" }` | 중괄호로 감싼 key-value 묶음 |
| 배열 (Array) | `[1, 2, 3]` | 대괄호로 감싼 값들의 나열 |
| null | `null` | 값이 없음을 명시 |

입력값 흐름 처리 (공격 시나리오 관점)
1. 개념
사용자가 입력한 값이 [사용자 → 브라우저 → 인터넷 → 서버 → DB → 다시 응답으로] 흘러가는 과정.
여기서 꼭 알아야 할 한 가지:

입력값은 사용자의 손을 떠나는 순간부터 "믿을 수 없는 값"이다.
공격자는 이 값을 마음대로 조작할 수 있기 때문에, 흐름의 각 길목에서 "이 값이 명령어로 둔갑하지 않게" 막는 게 보안의 핵심이다.

2. 특징
흐름은 크게 두 갈래로 나뉜다.

들어가는 길 (요청): 사용자 입력 → 브라우저 → 서버 → DB
나오는 길 (응답): DB 결과 → 서버 → 브라우저 → 사용자 화면
핵심 특징 3가지

입력값은 적이라고 가정한다. "설마 이렇게 보내겠어?" → 공격자는 보낸다.
클라이언트 검증 ≠ 보안. 브라우저(JS) 검증은 우회 가능 → 서버에서 반드시 다시 검증.
막는 위치가 다르다. SQLi는 DB로 보내기 전, XSS는 브라우저로 내보낼 때.

3. 시나리오 (단계별 흐름)
① [사용자] 값 입력

정상: id = kim
🔴 공격: id = admin'-- (SQLi) 또는 <script>...</script> (XSS)

② [브라우저] 입력값을 HTTP 요청에 담아 전송
httpPOST /login HTTP/1.1

id=admin'--&pw=1234

🔴 공격자는 브라우저를 안 거치고 직접 요청을 만들어 보낼 수 있음 → JS 검증만으론 못 막음

③ [인터넷] 요청 전송

🔴 HTTPS가 아니면 중간에서 가로채거나 변조 가능

④ [서버] req 객체로 입력값 받음 (예: req.body.id)

이 값은 공격자가 만든 값일 수 있음 → 정상이라 단정 금지

⑤ [서버 처리] DB 조회·조건 검사 ← 🔥 1차 위험 (SQL Injection)
sql-- 정상
SELECT * FROM users WHERE id='kim' AND pw='1234'
-- 공격: admin'-- 주입
SELECT * FROM users WHERE id='admin'--' AND pw='1234'
→ -- 뒤(비밀번호 검사)가 주석 처리 → admin 로그인 성공
🛡️ 방어: Prepared Statement로 쿼리 틀과 입력값 분리
⑥ [서버] 결과를 HTTP 응답에 담음 ← 🔥 2차 위험 (XSS)

🔴 입력값을 이스케이프 없이 응답에 그대로 넣으면 악성 스크립트가 응답에 실려 나감
🛡️ 방어: 출력 시 이스케이프(< → &lt;), textContent 사용

⑦ [인터넷] 응답 전송 → ⑧ [브라우저] 응답 받음
⑨ [사용자/피해자] 결과 화면 확인 ← 🔥 XSS가 실제로 실행되는 곳

🔴 브라우저가 응답을 렌더링하는 순간, 섞여 들어온 악성 스크립트가 피해자 브라우저에서 실행 → 쿠키·세션 탈취


💡 정리: SQLi는 ⑤에서 DB를 공격하고, XSS는 ⑥에서 심어져 ⑨에서 피해자 브라우저를 공격한다.